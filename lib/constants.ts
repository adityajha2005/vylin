export const DAILY_FREE_REQUEST_LIMIT = 5 as const;
export const MAX_QUESTION_LENGTH = 500 as const;
export const NORMAL_MODE_COST = 1 as const;
export const RESEARCH_MODE_COST = 2 as const;
export const ONCHAIN_MODE_COST = 3 as const;
export const MAX_OUTPUT_TOKENS = 800 as const;

export const ALLOWED_SEARCH_DOMAINS = [
  "docs.solana.com",
  "github.com/solana-labs",
  "github.com/anza-xyz",
  "anchor-lang.com",
  "docs.rs",
] as const;

export const DEFAULT_TIMEZONE = "UTC" as const;
